
var mocha = require('mocha');

var describe = mocha.describe;
var it = mocha.it;

describe('Client User Interface', function () {
  describe('why it should not be tested', function () {
    it('would be testing implementation details instead of behaviour mostly', function () {
    });
    it('the behaviour of units in this package are just to produce strings and to conform to renderer protocol', function () {
    });
    it('produces html strings, working tests can break from same functional output', function () {
    });
    it('would significantly degrade test confidence', function () {
    });
    it('would only serve to entrench ui code, the antithesis of the point of testing (ability to change code freely, easily and with confidence)', function () {
    });

    it('is debateable', function () {
    });
  });
});
