
var mocha = require('mocha');

var htmlElement = require('../../../../../../app/agents/client/framework/html/element');

var describe = mocha.describe;
var it = mocha.it;

describe('Client Framework: Html: Element', function () {
  it('should encapsulate html element querying, manipulation', function () {
  });
});
