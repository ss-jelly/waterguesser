
var chai = require('chai');
var mocha = require('mocha');

// var serverAgentEntryPoint = require('../../../app/agents/server');

var expect = chai.expect;
var describe = mocha.describe;
var it = mocha.it;

describe('Server Agent Entry Point', function () {
  it('should serve the client agent of the app', function () {
  });
});
